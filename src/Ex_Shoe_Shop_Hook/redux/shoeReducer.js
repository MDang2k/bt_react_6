import { dataShoe } from "../dataShoe"

let initialValue = {
    listShoe: dataShoe,
    cart: []
}

export const shoeReducer = (state = initialValue, action) => {
    switch (action) {
        // case 
        default:
            return state;
    }

}